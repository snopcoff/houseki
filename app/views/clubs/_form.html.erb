<%= form_for(@club) do |f| %>
  <% if @club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% @club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row">
    <div class="col-md-6">
      <div class="col-md-4"><%= f.label :name %></div>
      <div class="col-md-8"><%= f.text_field :name, style: "width: 100%;" %></div>
    </div>
    <div class="col-md-6">
      <%= f.label :club_avatar %><br>
      <%= f.file_field :club_avatar %>
      <%= f.hidden_field :club_avatar_cache %>
    </div>
    <div class="col-md-6">
      <div class="col-md-4"><%= f.label :description %></div>
      <div class="col-md-8"><%= f.text_area :description, style: "width: 100%;" %></div></br>
      <div class="actions col-md-4">
        <%= f.submit 'Create', :class => "btn btn-success" %>
      </div>
    </div>
    
  </div>
  
<% end %>
