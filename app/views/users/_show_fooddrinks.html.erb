<div class="row">
    <div class="col-md-12">
        <% @fooddrinks.each do |fooddrink| %>
            <div class="col-md-4 users-show-fooddrink">
                <%= link_to fooddrink_path(fooddrink.id), :rel => "lightbox", class: "has-tooltip img-thumbnail", :data => {:toggle=>"tooltip", :placement=>"auto"}, title: display_info(fooddrink) do %>
                    <%= image_tag(fooddrink.file, class: "avatar") %>
                <% end %>
            </div>
        <% end %>
    </div>
    <div class="col-md-6 col-md-offset-3">
        <%= will_paginate @fooddrinks %>
    </div>
</div>